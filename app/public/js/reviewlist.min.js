webpackJsonp([0],[
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _reviewlist = __webpack_require__(1);

	var _reviewlist2 = _interopRequireDefault(_reviewlist);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	// global __data
	var ReviewList = React.createFactory(_reviewlist2.default);
	ReactDOM.render(ReviewList(__data), document.getElementById('reviews-mount'));

/***/ },
/* 1 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _react = __webpack_require__(2);

	var _react2 = _interopRequireDefault(_react);

	var _auth = __webpack_require__(3);

	var _auth2 = _interopRequireDefault(_auth);

	var _makerequest = __webpack_require__(4);

	var _makerequest2 = _interopRequireDefault(_makerequest);

	var _generateStars = __webpack_require__(11);

	var _generateStars2 = _interopRequireDefault(_generateStars);

	var _reviewform = __webpack_require__(6);

	var _reviewform2 = _interopRequireDefault(_reviewform);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var ReviewsList = _react2.default.createClass({
	  displayName: 'ReviewsList',

	  makeRequest: _makerequest2.default,
	  getInitialState: function getInitialState() {
	    return {
	      isLoggedIn: false,
	      newReview: {
	        title: '',
	        owner_id: '',
	        owner_name: '',
	        restaurant_id: this.props.restaurant_id,
	        body: '',
	        stars: '1'
	      },
	      message: false
	    };
	  },
	  submitNewReview: function submitNewReview(review) {
	    var self = this;
	    var options = {
	      method: 'POST',
	      body: JSON.stringify(review),
	      headers: {
	        'Accept': 'application/json',
	        'Content-Type': 'application/json'
	      }
	    };

	    var endpoint = '/api/restaurant/' + this.state.newReview.restaurant_id;

	    this.makeRequest(endpoint, options).then(function (response) {
	      if (response.error) {
	        self.setState({ message: response.error });
	      } else {
	        window.location = window.location;
	      }
	    }).catch(function (err) {
	      console.log(err);
	    });
	  },
	  componentDidMount: function componentDidMount() {
	    var user = _auth2.default.getUser();
	    if (user) {
	      var newReview = this.state.newReview;
	      newReview.owner_id = user._id;
	      newReview.owner_name = user.email;

	      this.setState({
	        isLoggedIn: true,
	        newReview: newReview
	      });
	    }
	  },
	  handleSubmit: function handleSubmit(e) {
	    e.preventDefault();
	    this.submitNewReview({ 'reviews': [this.state.newReview] });
	  },
	  handleChange: function handleChange(e) {
	    e.preventDefault();
	    var updatedState = this.state.newReview;
	    var name = e.target.name;
	    updatedState[name] = e.target.value;
	    this.setState(updatedState);
	  },

	  generateStars: _generateStars2.default,
	  render: function render() {
	    var self = this;
	    var Reviews = this.props.reviews.map(function (review, index) {
	      var time = new Date(Date.parse(review.createdAt)).toDateString();
	      return _react2.default.createElement(
	        'article',
	        { className: 'review-container', key: index },
	        _react2.default.createElement(
	          'div',
	          { className: 'row' },
	          _react2.default.createElement(
	            'div',
	            { className: 'col-md-8' },
	            _react2.default.createElement(
	              'h3',
	              { className: 'review-title' },
	              review.title
	            ),
	            _react2.default.createElement(
	              'p',
	              null,
	              review.body
	            )
	          ),
	          _react2.default.createElement(
	            'aside',
	            { className: 'col-md-4 reviewstar-container' },
	            self.generateStars([self.props.reviews[index]]),
	            _react2.default.createElement(
	              'div',
	              { className: 'review-owner-name cb' },
	              'By ' + review.owner_name,
	              _react2.default.createElement('br', null),
	              'on ',
	              time
	            )
	          )
	        )
	      );
	    });

	    return _react2.default.createElement(
	      'section',
	      { className: 'mb' },
	      Reviews,
	      _react2.default.createElement(_reviewform2.default, {
	        restaurant_id: this.props.restaurant_id,
	        handleChange: this.handleChange,
	        handleSubmit: this.handleSubmit,
	        newReview: this.state.newReview,
	        isLoggedIn: this.state.isLoggedIn,
	        message: this.state.message })
	    );
	  }
	});

	exports.default = ReviewsList;

/***/ },
/* 2 */,
/* 3 */,
/* 4 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	__webpack_require__(5);

	var defaultOptions = {
	  headers: {
	    'Accept': 'application/json'
	  }
	};

	function makeRequest(endpoint, userOptions) {
	  var options = Object.assign(defaultOptions, userOptions);

	  if (options.method === 'get' || options.method === 'GET') {
	    delete options.body;
	  }
	  return fetch(endpoint, options).then(function (response) {
	    return response.json();
	  }).then(function (json) {
	    return json;
	  }).catch(function (ex) {
	    console.log('parsing failed', ex);
	  });
	}

	exports.default = makeRequest;

/***/ },
/* 5 */
/***/ function(module, exports) {

	(function(self) {
	  'use strict';

	  if (self.fetch) {
	    return
	  }

	  var support = {
	    searchParams: 'URLSearchParams' in self,
	    iterable: 'Symbol' in self && 'iterator' in Symbol,
	    blob: 'FileReader' in self && 'Blob' in self && (function() {
	      try {
	        new Blob()
	        return true
	      } catch(e) {
	        return false
	      }
	    })(),
	    formData: 'FormData' in self,
	    arrayBuffer: 'ArrayBuffer' in self
	  }

	  function normalizeName(name) {
	    if (typeof name !== 'string') {
	      name = String(name)
	    }
	    if (/[^a-z0-9\-#$%&'*+.\^_`|~]/i.test(name)) {
	      throw new TypeError('Invalid character in header field name')
	    }
	    return name.toLowerCase()
	  }

	  function normalizeValue(value) {
	    if (typeof value !== 'string') {
	      value = String(value)
	    }
	    return value
	  }

	  // Build a destructive iterator for the value list
	  function iteratorFor(items) {
	    var iterator = {
	      next: function() {
	        var value = items.shift()
	        return {done: value === undefined, value: value}
	      }
	    }

	    if (support.iterable) {
	      iterator[Symbol.iterator] = function() {
	        return iterator
	      }
	    }

	    return iterator
	  }

	  function Headers(headers) {
	    this.map = {}

	    if (headers instanceof Headers) {
	      headers.forEach(function(value, name) {
	        this.append(name, value)
	      }, this)

	    } else if (headers) {
	      Object.getOwnPropertyNames(headers).forEach(function(name) {
	        this.append(name, headers[name])
	      }, this)
	    }
	  }

	  Headers.prototype.append = function(name, value) {
	    name = normalizeName(name)
	    value = normalizeValue(value)
	    var list = this.map[name]
	    if (!list) {
	      list = []
	      this.map[name] = list
	    }
	    list.push(value)
	  }

	  Headers.prototype['delete'] = function(name) {
	    delete this.map[normalizeName(name)]
	  }

	  Headers.prototype.get = function(name) {
	    var values = this.map[normalizeName(name)]
	    return values ? values[0] : null
	  }

	  Headers.prototype.getAll = function(name) {
	    return this.map[normalizeName(name)] || []
	  }

	  Headers.prototype.has = function(name) {
	    return this.map.hasOwnProperty(normalizeName(name))
	  }

	  Headers.prototype.set = function(name, value) {
	    this.map[normalizeName(name)] = [normalizeValue(value)]
	  }

	  Headers.prototype.forEach = function(callback, thisArg) {
	    Object.getOwnPropertyNames(this.map).forEach(function(name) {
	      this.map[name].forEach(function(value) {
	        callback.call(thisArg, value, name, this)
	      }, this)
	    }, this)
	  }

	  Headers.prototype.keys = function() {
	    var items = []
	    this.forEach(function(value, name) { items.push(name) })
	    return iteratorFor(items)
	  }

	  Headers.prototype.values = function() {
	    var items = []
	    this.forEach(function(value) { items.push(value) })
	    return iteratorFor(items)
	  }

	  Headers.prototype.entries = function() {
	    var items = []
	    this.forEach(function(value, name) { items.push([name, value]) })
	    return iteratorFor(items)
	  }

	  if (support.iterable) {
	    Headers.prototype[Symbol.iterator] = Headers.prototype.entries
	  }

	  function consumed(body) {
	    if (body.bodyUsed) {
	      return Promise.reject(new TypeError('Already read'))
	    }
	    body.bodyUsed = true
	  }

	  function fileReaderReady(reader) {
	    return new Promise(function(resolve, reject) {
	      reader.onload = function() {
	        resolve(reader.result)
	      }
	      reader.onerror = function() {
	        reject(reader.error)
	      }
	    })
	  }

	  function readBlobAsArrayBuffer(blob) {
	    var reader = new FileReader()
	    reader.readAsArrayBuffer(blob)
	    return fileReaderReady(reader)
	  }

	  function readBlobAsText(blob) {
	    var reader = new FileReader()
	    reader.readAsText(blob)
	    return fileReaderReady(reader)
	  }

	  function Body() {
	    this.bodyUsed = false

	    this._initBody = function(body) {
	      this._bodyInit = body
	      if (typeof body === 'string') {
	        this._bodyText = body
	      } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
	        this._bodyBlob = body
	      } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
	        this._bodyFormData = body
	      } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
	        this._bodyText = body.toString()
	      } else if (!body) {
	        this._bodyText = ''
	      } else if (support.arrayBuffer && ArrayBuffer.prototype.isPrototypeOf(body)) {
	        // Only support ArrayBuffers for POST method.
	        // Receiving ArrayBuffers happens via Blobs, instead.
	      } else {
	        throw new Error('unsupported BodyInit type')
	      }

	      if (!this.headers.get('content-type')) {
	        if (typeof body === 'string') {
	          this.headers.set('content-type', 'text/plain;charset=UTF-8')
	        } else if (this._bodyBlob && this._bodyBlob.type) {
	          this.headers.set('content-type', this._bodyBlob.type)
	        } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
	          this.headers.set('content-type', 'application/x-www-form-urlencoded;charset=UTF-8')
	        }
	      }
	    }

	    if (support.blob) {
	      this.blob = function() {
	        var rejected = consumed(this)
	        if (rejected) {
	          return rejected
	        }

	        if (this._bodyBlob) {
	          return Promise.resolve(this._bodyBlob)
	        } else if (this._bodyFormData) {
	          throw new Error('could not read FormData body as blob')
	        } else {
	          return Promise.resolve(new Blob([this._bodyText]))
	        }
	      }

	      this.arrayBuffer = function() {
	        return this.blob().then(readBlobAsArrayBuffer)
	      }

	      this.text = function() {
	        var rejected = consumed(this)
	        if (rejected) {
	          return rejected
	        }

	        if (this._bodyBlob) {
	          return readBlobAsText(this._bodyBlob)
	        } else if (this._bodyFormData) {
	          throw new Error('could not read FormData body as text')
	        } else {
	          return Promise.resolve(this._bodyText)
	        }
	      }
	    } else {
	      this.text = function() {
	        var rejected = consumed(this)
	        return rejected ? rejected : Promise.resolve(this._bodyText)
	      }
	    }

	    if (support.formData) {
	      this.formData = function() {
	        return this.text().then(decode)
	      }
	    }

	    this.json = function() {
	      return this.text().then(JSON.parse)
	    }

	    return this
	  }

	  // HTTP methods whose capitalization should be normalized
	  var methods = ['DELETE', 'GET', 'HEAD', 'OPTIONS', 'POST', 'PUT']

	  function normalizeMethod(method) {
	    var upcased = method.toUpperCase()
	    return (methods.indexOf(upcased) > -1) ? upcased : method
	  }

	  function Request(input, options) {
	    options = options || {}
	    var body = options.body
	    if (Request.prototype.isPrototypeOf(input)) {
	      if (input.bodyUsed) {
	        throw new TypeError('Already read')
	      }
	      this.url = input.url
	      this.credentials = input.credentials
	      if (!options.headers) {
	        this.headers = new Headers(input.headers)
	      }
	      this.method = input.method
	      this.mode = input.mode
	      if (!body) {
	        body = input._bodyInit
	        input.bodyUsed = true
	      }
	    } else {
	      this.url = input
	    }

	    this.credentials = options.credentials || this.credentials || 'omit'
	    if (options.headers || !this.headers) {
	      this.headers = new Headers(options.headers)
	    }
	    this.method = normalizeMethod(options.method || this.method || 'GET')
	    this.mode = options.mode || this.mode || null
	    this.referrer = null

	    if ((this.method === 'GET' || this.method === 'HEAD') && body) {
	      throw new TypeError('Body not allowed for GET or HEAD requests')
	    }
	    this._initBody(body)
	  }

	  Request.prototype.clone = function() {
	    return new Request(this)
	  }

	  function decode(body) {
	    var form = new FormData()
	    body.trim().split('&').forEach(function(bytes) {
	      if (bytes) {
	        var split = bytes.split('=')
	        var name = split.shift().replace(/\+/g, ' ')
	        var value = split.join('=').replace(/\+/g, ' ')
	        form.append(decodeURIComponent(name), decodeURIComponent(value))
	      }
	    })
	    return form
	  }

	  function headers(xhr) {
	    var head = new Headers()
	    var pairs = (xhr.getAllResponseHeaders() || '').trim().split('\n')
	    pairs.forEach(function(header) {
	      var split = header.trim().split(':')
	      var key = split.shift().trim()
	      var value = split.join(':').trim()
	      head.append(key, value)
	    })
	    return head
	  }

	  Body.call(Request.prototype)

	  function Response(bodyInit, options) {
	    if (!options) {
	      options = {}
	    }

	    this.type = 'default'
	    this.status = options.status
	    this.ok = this.status >= 200 && this.status < 300
	    this.statusText = options.statusText
	    this.headers = options.headers instanceof Headers ? options.headers : new Headers(options.headers)
	    this.url = options.url || ''
	    this._initBody(bodyInit)
	  }

	  Body.call(Response.prototype)

	  Response.prototype.clone = function() {
	    return new Response(this._bodyInit, {
	      status: this.status,
	      statusText: this.statusText,
	      headers: new Headers(this.headers),
	      url: this.url
	    })
	  }

	  Response.error = function() {
	    var response = new Response(null, {status: 0, statusText: ''})
	    response.type = 'error'
	    return response
	  }

	  var redirectStatuses = [301, 302, 303, 307, 308]

	  Response.redirect = function(url, status) {
	    if (redirectStatuses.indexOf(status) === -1) {
	      throw new RangeError('Invalid status code')
	    }

	    return new Response(null, {status: status, headers: {location: url}})
	  }

	  self.Headers = Headers
	  self.Request = Request
	  self.Response = Response

	  self.fetch = function(input, init) {
	    return new Promise(function(resolve, reject) {
	      var request
	      if (Request.prototype.isPrototypeOf(input) && !init) {
	        request = input
	      } else {
	        request = new Request(input, init)
	      }

	      var xhr = new XMLHttpRequest()

	      function responseURL() {
	        if ('responseURL' in xhr) {
	          return xhr.responseURL
	        }

	        // Avoid security warnings on getResponseHeader when not allowed by CORS
	        if (/^X-Request-URL:/m.test(xhr.getAllResponseHeaders())) {
	          return xhr.getResponseHeader('X-Request-URL')
	        }

	        return
	      }

	      xhr.onload = function() {
	        var options = {
	          status: xhr.status,
	          statusText: xhr.statusText,
	          headers: headers(xhr),
	          url: responseURL()
	        }
	        var body = 'response' in xhr ? xhr.response : xhr.responseText
	        resolve(new Response(body, options))
	      }

	      xhr.onerror = function() {
	        reject(new TypeError('Network request failed'))
	      }

	      xhr.ontimeout = function() {
	        reject(new TypeError('Network request failed'))
	      }

	      xhr.open(request.method, request.url, true)

	      if (request.credentials === 'include') {
	        xhr.withCredentials = true
	      }

	      if ('responseType' in xhr && support.blob) {
	        xhr.responseType = 'blob'
	      }

	      request.headers.forEach(function(value, name) {
	        xhr.setRequestHeader(name, value)
	      })

	      xhr.send(typeof request._bodyInit === 'undefined' ? null : request._bodyInit)
	    })
	  }
	  self.fetch.polyfill = true
	})(typeof self !== 'undefined' ? self : this);


/***/ },
/* 6 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _react = __webpack_require__(2);

	var _react2 = _interopRequireDefault(_react);

	var _getmessage = __webpack_require__(7);

	var _getmessage2 = _interopRequireDefault(_getmessage);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

	var ReviewForm = _react2.default.createClass({
	  displayName: 'ReviewForm',

	  getMessage: _getmessage2.default,
	  renderForm: function renderForm() {
	    return _react2.default.createElement(
	      'form',
	      { onSubmit: this.props.handleSubmit },
	      _react2.default.createElement(
	        'h4',
	        null,
	        'Add Review'
	      ),
	      _react2.default.createElement(
	        'fieldset',
	        { className: 'form-inline' },
	        _react2.default.createElement(
	          'label',
	          { htmlFor: 'title', hidden: true },
	          'Title:'
	        ),
	        _react2.default.createElement('input', { name: 'title', type: 'text', value: this.props.newReview.title, placeholder: 'Title', onChange: this.props.handleChange, className: 'form-control', required: true }),
	        _react2.default.createElement(
	          'label',
	          { htmlFor: 'stars', className: 'ml' },
	          'Rating:'
	        ),
	        _react2.default.createElement(
	          'select',
	          _defineProperty({ className: 'form-control', name: 'stars', selected: '1', value: this.props.newReview.stars, placeholder: 'Stars', onChange: this.props.handleChange }, 'className', 'form-control'),
	          _react2.default.createElement(
	            'option',
	            null,
	            '1'
	          ),
	          _react2.default.createElement(
	            'option',
	            null,
	            '2'
	          ),
	          _react2.default.createElement(
	            'option',
	            null,
	            '3'
	          ),
	          _react2.default.createElement(
	            'option',
	            null,
	            '4'
	          ),
	          _react2.default.createElement(
	            'option',
	            null,
	            '5'
	          )
	        )
	      ),
	      _react2.default.createElement(
	        'fieldset',
	        { className: 'form-group' },
	        _react2.default.createElement(
	          'label',
	          { htmlFor: 'message', hidden: true },
	          'Message'
	        ),
	        _react2.default.createElement('textarea', { name: 'body', placeholder: 'Message', onChange: this.props.handleChange, className: 'form-control', rows: '3', value: this.props.newReview.body, required: true })
	      ),
	      _react2.default.createElement('input', { name: 'submit', type: 'submit', className: 'btn btn-primary btn-block' })
	    );
	  },
	  renderCreateOrLogin: function renderCreateOrLogin() {
	    return _react2.default.createElement(
	      'section',
	      { className: 'padding bg-info text-center' },
	      _react2.default.createElement(
	        'p',
	        null,
	        'Please ',
	        _react2.default.createElement(
	          'a',
	          { href: '/user/login', className: 'btn btn-primary' },
	          'Login'
	        ),
	        ' or ',
	        _react2.default.createElement(
	          'a',
	          { href: '/user/signup', className: 'btn btn-primary' },
	          'Create Account'
	        )
	      )
	    );
	  },
	  showForm: function showForm() {
	    return this.props.isLoggedIn ? this.renderForm() : this.renderCreateOrLogin();
	  },
	  render: function render() {
	    return _react2.default.createElement(
	      'section',
	      { id: 'restaurantform-mount', className: 'mt' },
	      this.showForm(),
	      this.getMessage(this.props.message)
	    );
	  }
	});

	exports.default = ReviewForm;

/***/ },
/* 7 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _react = __webpack_require__(2);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function getMessage(message) {
	  var messageState = message;

	  var MessageContainer = _react2.default.createElement(
	    'section',
	    { className: 'padding text-center text-warning bg-danger' },
	    message
	  );
	  return message ? MessageContainer : false;
	}

	exports.default = getMessage;

/***/ },
/* 8 */,
/* 9 */,
/* 10 */,
/* 11 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _react = __webpack_require__(2);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function generateStars(reviews) {
	  var results = reviews.map(function (review, index) {
	    var stars = [];
	    var max = 5 - review.stars;
	    var i = -1;
	    var ii = -1;
	    while (++i < review.stars) {
	      stars.push(_react2.default.createElement('span', { className: 'review-star', key: 'b' + i }));
	    }

	    while (++ii < max) {
	      stars.push(_react2.default.createElement('span', { className: 'review-star review-star-gray', key: 'a' + ii }));
	    }
	    return stars;
	  });

	  return results;
	}

	exports.default = generateStars;

/***/ }
]);